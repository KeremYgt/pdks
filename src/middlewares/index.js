const swagger = require('./swagger');
const authorization = require('./authorization');
const admin = require('./admin');
const cron = require('./cron');

module.exports = { swagger, authorization, admin, cron };
