const db = require('../models');

module.exports = {
    create: async (izin) => {
        const result = await db.collection('izin').insertOne(izin);
        return result.ops ? result.ops[0] : izin;
    },

    getAll: async () => {
        return await db.collection('izin').find({}).toArray();
    }
};
